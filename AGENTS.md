# RONPA 開発ガイド

## このリポジトリの扱い

RONPA は、ディベートの実践と AI 採点を提供するサービス。ユーザーは友人と共同で開発・運用し、技術・開発を担当している。このファイルは、今後の開発を支援するエージェントの共通指針とする。

正規リポジトリは `Ronpa-org/ronpa`。`origin` は `git@github.com:Ronpa-org/ronpa.git` を使用する。`main` の変更は作業ブランチからPRで行い、保護ルールを迂回しない。CIと設定確認の手順は `docs/development.md` を参照する。

ユーザー向けの現在地・合意事項・担当・次の作業は `docs/progress.md` に記録する。関連作業で状況が変わったら、確認日と根拠を添えて更新する。当面Vercel／Renderは友人が既存環境を管理し、ユーザーはGitHubで開発する。管理画面への招待や招待目的の有料化を開発の前提にせず、再接続確認は友人への依頼を起点にする。

**このリポジトリは古く、不完全な開発途中のスナップショットである。既存コードは現状を知る証拠であり、今後の正しい仕様や本番稼働の証明ではない。**

- ユーザーの現在の指示・合意した仕様を起点にする。過去の README、画面文言、コメント、資料だけで現行要件を確定しない。
- 調査・報告では「コードで確認できたこと」「実際に動作確認したこと」「未確認・提案」を区別する。画面や接続コードがあるだけで完成済みと判断しない。
- 不明点があっても、調査や可逆的な作業は進める。実装判断を左右する不足情報だけを具体的に質問する。
- 作業前に `git status --short` と対象範囲の指示を確認する。ユーザーの変更や未追跡の資料を上書き・削除しない。
- このファイルはリポジトリ全体に適用する。`frontend/web/` では、その配下の `AGENTS.md` も読む。

## 説明・相談の進め方

- 原則として日本語で、必ず「全体構造・原理 → 細部の事実」の順に説明する。
- ユーザーはパターン推論・構造把握が得意。用語や事例の暗記を求めず、「なぜそうなるか」を関係・規則・因果で示す。
- 新しい概念は、既知の概念との「似ている点・違う点」を使って説明する。
- 判断を求めるときは、何が変わるか、選択肢の違い、推奨理由を簡潔に示す。
- 完了報告は、変更内容・理由・検証結果・残る未確認事項をまとめる。実行していない検証を成功扱いしない。

## 全体構造

現状は、ブラウザーから二つの接続先を使う構成。FastAPI がすべてのデータアクセスを仲介しているわけではない。

```text
ブラウザー / Next.js
  ├─ HTTP → FastAPI → OpenAI（AI対戦・AI採点）
  │            └─ プロセスメモリー（AI対戦セッション）
  └─ Supabase JS → Auth / DB / Storage / Realtime
                   （認証・戦績・プロフィール・画像・対人通信）
```

| 場所 | 責務・読む目的 |
| --- | --- |
| `frontend/web/app/` | Next.js App Router の画面・レイアウト |
| `frontend/web/components/` | ロゴ、ナビゲーション、採点チャートなどの共通 UI |
| `frontend/web/lib/api.ts` | AI対戦 API クライアント・型・結果保存キー |
| `frontend/web/lib/versus.ts` | 対人戦の型・部屋コード生成・採点 API クライアント |
| `frontend/web/lib/supabase.ts`, `auth.tsx` | Supabase クライアントと認証状態 |
| `frontend/web/lib/battles.ts`, `profile.ts` | 戦績・プロフィール・アバターの読み書き |
| `frontend/web/lib/i18n.tsx` | 日本語・英語の UI 文言、言語状態、サンプルデータ |
| `frontend/web/lib/mock.ts` | 固定のデモデータ。利用箇所は変更前に検索する |
| `frontend/web/lib/legal.ts` | 利用規約・プライバシーポリシーのドラフト |
| `backend/app/main.py` | FastAPI ルート、入力モデル、CORS、HTTP エラー |
| `backend/app/debate.py` | テーマ、難易度、対戦セッション、AI生成・採点・モック |
| `backend/requirements.txt` | Python 依存関係。下限指定で、ロックファイルはない |
| `render.yaml` | バックエンドの Render デプロイ定義 |
| `.github/workflows/ci.yml` | フロントのLint・型・ビルド、バックエンドAPIテスト |
| `backend/tests/` | 外部接続なしで実行するAPI回帰テスト |
| `docs/development.md` | ブランチ保護、CI、デプロイ連携の確認手順 |
| `docs/progress.md` | ユーザー向け進捗、担当分担、合意事項、次の作業 |
| `資料_v0/` | ローカルにある過去資料 PDF。現行仕様とは扱わず、必要時に内容・時点を確認する |

### 実装の入口と到達範囲

以下は 2026-09-25 にコードを読んで確認した状態。外部サービスを含む動作保証ではない。

| 機能 | 入口・処理の流れ | 現状の制約 |
| --- | --- | --- |
| AI対戦 | `/room?mode=ai` → `AiRoomClient.tsx` → FastAPI | テキスト対戦。難易度4段階、肯定・否定、日本語・英語 |
| AI対戦の結果 | 採点 → `sessionStorage` → `/result` | ログイン中は `battles` に保存を試みる。保存失敗は警告のみ |
| 対人戦 | `/versus` → `/versus/room` → Supabase Realtime | 6桁コード、Presence/Broadcast。ホストが採点 API を呼び結果を配信 |
| 認証 | `/auth` → Supabase Auth | メール・パスワード。Supabase 未設定では認証不可、ゲスト利用の導線あり |
| 戦績・プロフィール | `/mypage`, `/mypage/settings` → Supabase | `battles`, `profiles`, Storage の `avatars` に依存 |
| グループ風ルーム | `/room` の AI 以外 → `RoomClient.tsx` | タイマー・固定参加者・マイク切替表示などのデモ。音声通信実装と誤認しない |
| ランダム対戦・公開ルーム・課金 | 対戦選択、ホーム、プラン表示など | 準備中文言や固定表示がある。マッチング・Stripe連携・利用回数制限の実装は確認できない |

`/result` は保存された結果がなければサンプルを表示する。グループ風ルームからの遷移でも直前の AI 結果が残っていればそれを表示し得るため、画面を見ただけで当該対戦の採点成功と判断しない。

## 古い記述・不足している基盤

- ルート README の「Supabase — 予定」は実装と一致しない。接続コードはあるが、SQL、マイグレーション、RLS（行単位のアクセス制御）、Storage ポリシーの定義はリポジトリにない。
- `getBattles()` と `getProfile()` はユーザー ID で明示的に絞り込まず、DB 側のアクセス制御に依存する。コメントの「本人のみ」を設定済みの証拠にしない。
- FastAPI に認証トークン検証、対戦セッションの所有者確認、レート制限の実装は見当たらない。フロントのログイン状態や CORS はこれらの代わりにならない。
- AI対戦セッションは `_sessions` 辞書だけに保存され、再起動で消える。永続化・期限切れ削除はなく、複数プロセス間でも共有されない。
- 対人戦はブラウザーが参加者・発言・採点結果を扱う。サーバーによる参加資格・人数・役割の検証、履歴の永続化、再接続時の復元は完成済みと扱わない。
- OpenAI のキーが未設定、または実装上の簡易チェックで無効と判定されるとモックになる。API 呼び出し失敗時にもデモ応答・デモ採点へフォールバックする。
- `/api/health` の `mock` は起動時のキー判定であり、OpenAI への接続確認ではない。個々の採点結果に実処理／モックの識別フィールドはなく、HTTP 成功だけで実際の AI 採点成功とは判定できない。
- `backend/.env.example` の Supabase・Stripe 変数は、現状のバックエンドコードでは参照されていない。
- `frontend/web/README.md` は主に Next.js の雛形説明。`.claude/launch.json` の `school-web` は別のローカル絶対パスを参照しており、このリポジトリ内の構成ではない。
- APIのモックテストとCIは開発基盤として追加したが、Supabase・OpenAIとの実接続や対人戦の自動E2Eテストは未整備。フロントの環境変数サンプルも未整備。README のデプロイ先やコード内の URL は、本番環境・所有者・稼働状態の証明ではない。

これらは今後の調査・整備対象であり、一つの開発タスクで無断にすべて作り替える指示ではない。関連箇所を変更するときに、必要な基盤と要件を確認する。

## 開発環境

### バージョンと依存関係

- `frontend/web/package.json` は Next.js `16.2.10`、React `19.2.4`、Tailwind CSS `4`、TypeScript `5` 系を指定。`package-lock.json` があるため npm を使用し、再現用インストールは `npm ci` を使う。
- Node.js はルートの `.nvmrc` で22系を指定し、CIも参照する。ローカル実行時はバージョンを確認する。
- `render.yaml` は Python `3.12.7` を指定。ローカルの Python が同じとは限らないため、`python3 --version` を確認し差分を記録する。
- 依存関係の更新・ロックファイルの再生成は、必要性がある変更として扱い、別の修正に混ぜない。
- フロントのコードを書く前に `frontend/web/AGENTS.md` に従い、`node_modules/next/dist/docs/` の関連ガイドを読む。依存未導入なら導入後に確認する。同梱ガイドが存在しない場合はその事実を示し、対象バージョンの公式情報を確認する。

### ローカル起動

以下は既存設定に基づく手順。既存の環境ファイルを上書きしない。

```bash
# リポジトリルートから。バックエンド用ターミナル
cd backend
python3 -m venv venv
./venv/bin/pip install -r requirements.txt
# .env が必要なら .env.example を元に作成。モック確認時は OPENAI_API_KEY を空にする
./venv/bin/uvicorn app.main:app --reload --port 8000
```

```bash
# リポジトリルートから。フロントエンド用の別ターミナル
cd frontend/web
npm ci
npm run dev
```

Supabase 未設定でも画面表示やモック API による AI対戦の確認は可能。ただし認証・DB保存・対人通信の確認には対応する外部設定が必要。

### コードが参照する環境変数

| 設定場所 | 変数 | 用途・未設定時の挙動 |
| --- | --- | --- |
| `backend/.env` / サーバー環境 | `OPENAI_API_KEY` | AI生成・採点。未設定時はモック |
| 同上 | `FRONTEND_ORIGINS` | CORS 許可元をカンマ区切りで指定。既定は `http://localhost:3000` |
| `frontend/web/.env.local` / フロントビルド環境 | `NEXT_PUBLIC_API_URL` | API 接続先。既定は `http://localhost:8000` |
| 同上 | `NEXT_PUBLIC_SUPABASE_URL` | Supabase プロジェクト URL |
| 同上 | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ブラウザー用公開キー。URL と両方そろうとクライアントを生成 |
| 同上 | `NEXT_PUBLIC_SITE_URL` | メタデータ用 URL。既定値は `app/layout.tsx` を確認 |

`NEXT_PUBLIC_*` はブラウザーに公開される設定として扱う。OpenAI の秘密鍵や Supabase の service role キーを入れない。実際のキーや個人情報はコード・ドキュメント・検証ログへ転記しない。

## 変更時の原則

- まず画面 → クライアント → API／外部サービス → 保存先の流れを追い、変更の影響範囲を決める。小さく確認できる単位で実装する。
- AI対戦 API の変更は `lib/api.ts` と `backend/app/main.py`・`debate.py` を照合する。対人採点は `lib/versus.ts` と `/api/versus/score` を照合する。TypeScript の型は実行時のレスポンス検証ではない。
- 現行の採点は `logic / persuasion / rebuttal / structure` の各0〜10点、合計40点。AI戦の勝者は `user / ai`、対人戦の勝者・立場は `肯定 / 否定`。英語表示でも内部値を不用意に変えない。
- 文言追加・変更は `lib/i18n.tsx` の日本語・英語をそろえる。既存のサンプル値や料金表示を実績・実装済み機能の根拠にしない。
- UI は既存の `app/globals.css` のテーマ、共通コンポーネント、モバイル中心のレイアウトを基準にする。ブラウザー API を使う処理はクライアント境界と初期表示を確認する。
- Supabase を変更するときは、対象テーブル／バケット、所有者、読み書き権限、既存データへの影響を確認する。新たに定義するスキーマやポリシーは、再現可能な SQL・設定手順としてリポジトリに残す。
- デモを削除する・実処理へ置き換えるときは、未設定時、通信失敗時、空データ時の挙動も定義する。モック成功と実サービス成功を区別できるようにする。
- デプロイや既存の外部 DB・ストレージの変更は、対象環境と今回の依頼範囲を確認する。ローカルのコード変更から本番反映までを自動的に同一視しない。
- 構成・コマンド・環境変数・仕様が変わったら、関連ドキュメントとこのファイルの現状記述も更新する。

## 検証と完了条件

変更に対応する検証を実施する。無関係な失敗があれば、今回の変更による失敗と分けて報告する。

| 変更対象 | 検証 |
| --- | --- |
| ドキュメントのみ | 記載したパス・スクリプト・環境変数と実ファイルを照合し、差分の誤記・不要変更を確認 |
| フロントのコード | `frontend/web/` で `npm run lint -- --max-warnings=0`、`npm run typecheck`、`npm run build`。対象画面と関連導線を確認 |
| バックエンドのコード | `backend/` で `requirements-dev.txt` を導入し、`./venv/bin/python -m compileall -q app tests` と `./venv/bin/python -m unittest discover -s tests -v`。ロジック変更には必要な回帰テストを追加 |
| AI対戦・採点 | キーを空にしたモック環境で、開始 → 発言 → 採点 → 結果表示。日本語／英語、肯定／否定、変更対象の難易度を確認 |
| Supabase の認証・保存 | テスト環境でログイン／ログアウト、保存／再取得、別ユーザーからのアクセス制限、未設定・失敗時を確認 |
| 対人通信 | 二つの独立したブラウザーセッションで、参加 → 双方の送受信 → 終了 → 双方の結果表示。切断・採点失敗も対象に応じて確認 |

- フロントの `npm test` や pytest のスイートはない。バックエンドは標準ライブラリの `unittest` を使う。存在しない検証コマンドを実行済みとしない。構文チェックだけで API の正常動作を保証しない。
- `next/font/google` を使っているため、ビルドはフォント取得のネットワーク制約でも失敗し得る。実際のエラーに基づいて原因を区別する。
- モック確認は実際の OpenAI・Supabase 接続確認を代替しない。実サービス検証は対象環境・認証情報・費用の前提を確認して行う。
- 過去の成功結果を今回の検証として流用せず、対象コミット・実行環境・実行したチェック・未確認事項を記録する。
